# Visualizations for things relating to bivariate generating functions.

## Setup for interactive visualizations:
- `pip -m venv env`
- `source env/bin/activate`
- `pip install -r requirements.txt`
- `python <filename>`
